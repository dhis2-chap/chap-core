{{- if .Values.dhis2.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chap.fullname" . }}-dhis2
  labels:
    {{- include "chap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  hostname: {{ required "dhis2.hostname is required when dhis2.enabled is true" .Values.dhis2.hostname | quote }}
  username: {{ .Values.dhis2.username | quote }}
  password: {{ required "dhis2.password is required when dhis2.enabled is true" .Values.dhis2.password | quote }}
{{- end }}
