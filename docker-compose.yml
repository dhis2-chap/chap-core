version: '3'
services:
  chap:
    build: .
    image: chap
    ports:
      - "8000:8000"
    working_dir: /

  chap-interface:
    build: climate_health/web_interface
    image: chap-interface
    container_name: chap-interface
    ports:
      - "4000:80"